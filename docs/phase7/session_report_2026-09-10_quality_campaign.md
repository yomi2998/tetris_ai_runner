# Final report: quality campaign resolves the migration decision

Date: 2026-09-10.
Sole outcome: the pre-declared quality criteria FAILED, and per the frozen mapping the migration stops. The legacy engine remains in production.

## What was measured

Following the owner's choice of the quality-first path, the missing harness was built and the full staged protocol executed:

- Harness (`75ba63b`): a per-seat engine abstraction over the existing tuner match machinery. `MatchJob` gained engine ids defaulting to legacy (behavior-identical; proven by a zero-mismatch `migration_compare` rerun). `ValueSeat` (`src/quality_seats.h`) drives the value engine, TOJ policy, and value pathfinder inside the shared battle rules with replay-validated paths and per-seat failure counters. `quality_match` (`tests/quality_match.cpp`) provides match, parity, and selftest modes with the pre-declared Wilson bounds (one-sided z = 1.6449 criterion, two-sided z = 1.96 context).
- Verification gates: 512-pair legacy parity with field-for-field `migration_compare` agreement and zero rerun mismatches; 128-pair value self-parity, zero mismatches; directed accounting fixtures (attack scoring, garbage rows, hold semantics); 28 named checks, 0 failures.
- Stages, each one fail-closed collector invocation: smoke 32 pairs (`041be1e`), screen 128 pairs (`b6357e1`), final 1000 fresh pairs (`c676532`). All at equal 20 ms per-seat budgets, CPU 15 offline per stage, zero replay failures and zero arena exhaustions throughout.

## The result

Final campaign, 2000 games, seed 999:

- Win-equivalence: value 804.0 of 2000 = 40.20 percent; one-sided 95 percent lower bound 0.3841 against the 0.47 non-inferiority criterion: FAIL. The two-sided interval [0.3807, 0.4237] lies entirely below 0.50: a statistically significant loss, consistent with the smoke (42.2%) and screen (43.4%) samples.
- APP: 1.027x legacy: pass.
- APL: 1.081x legacy: pass.

The value engine plays more aggressively (higher attack per piece and per line) but converts that into more deaths, not wins: 1196 versus 804 deaths, 620 of them spawn deaths under incoming garbage.

## The decision, per the frozen mapping

`NON-INFERIORITY-FAILED` stops the migration. The legacy engine stays in production. No qualification, cutover, production change, quality-campaign extension, or legacy deletion follows.

## Final state of the program

- The migration question is now closed with evidence on every axis: performance (binding 1.04155 against 1.02; four `NO-ADVANCE` optimization trials), quality (decisive 40.2 percent versus the required lower bound of 0.47), and legacy-host feasibility (memory-infeasible at real-workload scale).
- The entire campaign preserved fail-closed discipline end to end: frozen artifacts untouched, the normal production binary byte-identical to `1dccaa808044593bd97d6e8af09848ce8b19e75b9dcefa50d63a17a6381db765` throughout, no legacy evidence consumed beyond the owner-authorized blocks, and every generated byte preserved on every terminal path.
- HEAD `c676532` on `fast-reachability-migration`; tracked tree clean; unrelated untracked research documents untouched; CPU 15 online.
- All candidate code, diagnostics, and evidence remain preserved on the branch and in `results/phase7/` should the owner ever reopen the question with a different engine design.
