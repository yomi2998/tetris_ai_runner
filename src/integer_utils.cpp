
// integer_utils.cpp
// All helpers are now inline in integer_utils.h, backed by <bit> (std::popcount /
// std::countr_zero) with an SSE4.2 fast path. This translation unit is kept for
// build compatibility.
